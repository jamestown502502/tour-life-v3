# RESUME PROMPT — Tour Life v3: UX/QA Fix Pass (Readability, Navigation, Cross-Platform)

**Paste this to Claude Code at the start of the next session.** Read `HANDOFF.md`, `CLAUDE.md`, `DESIGN.md`, and `POLISH_CLOSE_OUT_PLAN.md` first, then follow this prompt exactly. This pass fixes three user-reported defects on the live build (tour-life-v3.vercel.app) — **readability/contrast, navigation/Continue stuck-states, and cross-platform behavior** — and delivers a documented testing workflow. **No new cities, no reality layer, no audio-file swap, no stack changes.**

---

```text
You are fixing three user-reported defects on "Tour Life: International Dates" (tour-life-v3, Vite + TS strict + Phaser 3.90, 4 cities, ~64 tests, CI green). The live build has: (1) white/light text that is hard to read across menu screens, explainer screens, and painted backgrounds; (2) "Continue" buttons that fail or leave the app stuck on certain screens; (3) unclear standards for how the game should behave across desktop web, iPhone, and Android before the Capacitor port. Fix all three, prove each with live verification, and write the cross-platform testing workflow as a doc.

GROUND TRUTH (verified against the code — trust this):
- src/ui/textStyles.ts defines the text system. Problem colors: `body`, `button`, `stat`, `title` use PALETTE_HEX.cream (#F5EBDD); `h2` and `small` use sky (#8FB7C9); `h1` uses gold (#D9A441). Measured WCAG contrast ratios on the game's own palette: cream on sand #E8D5B7 = 1.22:1; sky on sand = 1.50:1; gold on sand = 1.57:1; cream on gold = 1.91:1; cream on sky = 1.82:1. All far below WCAG AA (4.5:1 body, 3:1 large). The painted Gemini backgrounds are light warm gouache, so light text on them is illegible. High-contrast pairs that DO pass: plum #4A2C40 on sand = 8.48:1, plum on cream = 10.32:1, cream on night #2B3A55 = 9.68:1. Dialogue body already uses plum-on-sand (passes). The fix is a systematic contrast pass, not a single color swap.
- TitleScene.resumeTarget(progress) maps progress.screen -> scene: title/bandCreator/routePlan map directly; 'city' needs cityId and passes `phase: progress.nodeId` (CityScene sanitizes unknown phases to 'arrival'); hub/rhythm/results/settings default to Hub; scrapbook maps to Scrapbook. "Continue" runs hasSave() -> loadRun() -> State.data = saved -> resumeTarget -> goTo. The 3-slot save picker and overwrite modals also call resumeTarget. Stuck-state candidates to investigate: a save whose progress.screen is a value added after the save was written (e.g. a minigame/calibration phase or any new screen), a save that fails isValidRunState (new additive fields must be backfilled by save.ts migrate()), the floating seed <input> intercepting taps near the Continue button region, a modal (overwrite/saves/gallery/HowToPlay) not closing before navigation, or a scene whose create() throws on resume because init() data is missing.
- Platform facts already handled correctly and must NOT be regressed: viewport-fit=cover + env(safe-area-inset-*) CSS, Scale.FIT 720x1280, multi-touch (activePointers:4), audio unlock on first gesture, 44px touch targets audited, IndexedDB + localStorage fallback save, PWA manifest + SW registered, fonts loaded before boot.

WORKSTREAM 1 — FULL READABILITY & CONTRAST AUDIT (fix issue #1).
 1a. BUILD THE AUDIT FIRST, then fix: write scripts/contrast-audit.mjs that, for every textStyle preset (title/h1/h2/speaker/dialogue/body/button/stat/small) x every surface it can appear on (cream, sand, night, plum, teal, terracotta, gold, sky, softRed + the painted bg family), computes WCAG relative-luminance contrast and prints PASS/FAIL against 4.5:1 (body) and 3:1 (large text >= 24px or >= 18.66px bold). Commit it and its output (docs/contrast-audit.md).
 1b. FIX THE TEXT SYSTEM so every preset has at least one guaranteed-passing pairing, applied per surface:
    - On light painted backgrounds and sand/cream panels: use PLUM (or a darker brown/night variant) for body/button/stat text — NOT cream/sky/gold. Dialogue already passes; extend the same principle to menu screens (Title buttons' labels, RoutePlan descriptions, Hub stat labels, Scrapbook body, HowToPlay, HelpButton panel, Saves/Gallery panels, minigame text).
    - Buttons: cream label text only on dark fills (teal/plum/night — verify each fill passes 4.5:1 with its label; if a fill is light (sky/gold/terracotta), use plum/night label instead). Compute and enforce per-fill label color, not per-button.
    - Where text must sit directly over painted art (Title tagline, HowToPlay pages, HelpButton one-liners, location picker headers): either (a) strengthen the existing scrim to a measured dark overlay under the text (document the alpha), or (b) put text on a sand/cream panel, or (c) use night/plum text with a stronger shadow. No bare light text on painted art.
    - Bump the truly tiny sizes: `small` 14px and `stat` 15px at 0.5417 phone scale are ~7.6-8.1 CSS px — below readable. Raise small -> 16px and stat -> 17px where they carry meaning (keep pure-decorative sizes where they are).
 1c. SCREEN-BY-SCREEN EVIDENCE: walk every screen (Title, BandCreator, RoutePlan, Hub, City arrival/locations/relationship/preshow, Rhythm HUD + cue banners, Results, Scrapbook, Settings, HowToPlay, HelpButton, Saves/Gallery panels, all 3 minigames) at 390x844, screenshot each, and mark each text element PASS/FAIL in docs/contrast-audit.md. Fix every FAIL you find, re-screenshot, annotate before/after.
 DONE-WHEN: contrast-audit.mjs reports PASS for every text preset on its actual surfaces; every screenshot at 390x844 is legible; docs/contrast-audit.md has the before/after table; no bare light text on painted backgrounds.

WORKSTREAM 2 — NAVIGATION & "CONTINUE" STUCK-STATE FIXES (fix issue #2).
 2a. REPRODUCE FIRST, document what you find in docs/navigation-fixes.md: fresh-save flow, mid-run save -> Continue, each manual save slot -> Load, and resume from each progress.screen value. Capture console errors (browser_console_messages) and the exact screen where it sticks. Check whether isValidRunState rejects any save (missing/extra fields), whether the seed <input> overlaps the Continue hit area, whether any modal stays open across goTo(), and whether CityScene.init() throws when phase/nodeId is a value from a newer save.
 2b. HARDEN resumeTarget + resume: add explicit cases for any new progress.screen values that can exist in a save (minigame/calibration/relationship/backlog — whatever CityScene and MiniGameScene write), sanitize CityScene resume data defensively (a valid cityId must ALWAYS be present or default to Hub), and make loadRun() treat a structurally-invalid save as "no save" (return null) rather than a partially-set state. Add an always-reachable escape: every screen that can be resumed into (City, Hub, MiniGame, Rhythm) must have a working Back/exit path, and if a scene's create() throws during resume, catch it and land on Title (never a frozen screen).
 2c. REGRESSION TESTS: add tests that (i) call resumeTarget for EVERY possible progress.screen value and assert a valid scene key + cityId present for 'city'; (ii) round-trip an old-schema save and a new-schema save through loadRun/saveRun and assert both resume; (iii) walk the golden path and a mid-minigame save -> Continue and assert it reaches a playable state. Add to CI (already running).
 2d. LIVE VERIFY: on desktop AND at iPhone 12/14 + Pixel 7 viewports — Continue from a fresh save, a mid-city save, a mid-minigame save, a mid-rhythm save, and a scrapbook save; each must land on a playable screen with no console errors.
 DONE-WHEN: every resume path lands on a playable screen at all three viewports; the seed-input/modal overlap hypotheses are either proven innocent (documented) or fixed; new tests green; CI green.

WORKSTREAM 3 — CROSS-PLATFORM STANDARDS + TESTING WORKFLOW (answer issue #3).
 3a. WRITE docs/CROSS_PLATFORM_TESTING.md — the standards + workflow doc, covering:
    - BEHAVIOR CONTRACT: state/progression/saves are identical across desktop web, iPhone Safari, Android Chrome (and the later Capacitor WebView); the ONLY allowed differences are input (touch vs mouse/keyboard), safe-area insets, audio-unlock gesture timing, and PWA installability. No platform-conditional game logic.
    - STANDARDS (each with its current status in this codebase, verified not assumed): viewport-fit=cover + env(safe-area-inset-*) in use; Scale.FIT with devicePixelRatio; touch targets >= 44 CSS px at 390px width (audited — re-verify after any UI change with the measured 0.5417x method); multi-touch for rhythm; audio unlock on first gesture; no hover-only critical actions (hover is progressive enhancement); portrait orientation for the game view; IndexedDB persistence with localStorage fallback; font preload before boot; no console errors in production.
    - TESTING WORKFLOW pre-port: (1) Playwright device-emulation matrix — iPhone 12, iPhone 14, Pixel 7, a small 360x740 Android — run a smoke suite: boot, New Run, Continue (all resume states), one full city loop, one rhythm song (touch taps + autoplay), one minigame, Settings toggles, save slots, scrapbook export, PWA installability check (manifest + SW + Add-to-Home-Screen simulation), offline reload. (2) A manual REAL-DEVICE checklist for iPhone Safari and Android Chrome via the Vercel link (safe-area visuals, audio unlock, touch feel, haptics on/off, PWA install, orientation). (3) Before any Capacitor port: run the same smoke suite against the built dist/ (the WebView is closer to a bundled app than to the dev server).
    - A CI job (new or extended): run the Playwright smoke suite at the 4 viewports on push/PR so platform regressions fail the build.
 3b. IMPLEMENT the CI smoke job (Playwright is already used for manual verification; add it as a proper CI step with the device matrix) and make the manual checklist a real file an owner can follow.
 DONE-WHEN: docs/CROSS_PLATFORM_TESTING.md is complete and accurate against the code; the CI smoke job runs the 4-viewport suite and is green; the real-device checklist is copy-paste usable.

FINAL VERIFY & SHIP:
 1. npm run typecheck && npm test — all green (existing + new tests from 2c).
 2. Full Playwright pass at 390x844 + 430x932 + desktop: every screen legible (Workstream 1 evidence), every resume path playable (Workstream 2), no console errors.
 3. Screenshot before/after to docs/polish-before-after/ (readability pass) + docs/contrast-audit.md + docs/navigation-fixes.md + docs/CROSS_PLATFORM_TESTING.md committed.
 4. Deploy via git push (Vercel auto-deploys; do NOT race it with a manual vercel deploy — confirm via list_deployments if unsure). curl the live URL: expect 200 + title "Tour Life: International Dates".
 5. Re-verify the specific complaints on the LIVE url after deploy: menu/explainer text readable; Continue works from a real saved run; no stuck screens.

GUARDRAILS:
- No new cities, no Part 3 reality layer, no audio-file swap, no engine/framework changes. This is a fix + documentation pass.
- Keep all accessibility settings + no-fail guarantees intact; re-verify the all-miss rhythm run still reaches Results after ANY RhythmScene change.
- Keep the painted-world/code-drawn-UI art rule. Do not asset-ify UI; do not change the visual style, only contrast/legibility.
- Match existing style; surgical changes; constants only in src/const.ts; any new Scene array/Map/Set field resets in init(); any scene with a DialogueBox un-ducks music in its own SHUTDOWN.
- Do NOT silently claim a fix: every defect in the three complaints must have a screenshot or console-log before/after as evidence, and anything you cannot verify (e.g. real iPhone hardware) must be listed in the manual real-device checklist instead of claimed.

FINISH with: files changed, new tests + count, per-workstream evidence (contrast table, navigation repro notes, cross-platform doc), deploy verification, and the honest list of what still needs a real device to confirm.
```
